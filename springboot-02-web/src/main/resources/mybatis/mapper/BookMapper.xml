<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.huang.mapper.BookMapper">

    <select id="getAllBooks" resultType="Book">
        select * from books
    </select>

</mapper>